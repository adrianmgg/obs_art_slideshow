import { SlideshowTheme } from './slideshow_theme';

/** contains all the globalish stuff we need to pass around */
// TODO give this a better name
export interface Globals {
	theme: SlideshowTheme;
};

